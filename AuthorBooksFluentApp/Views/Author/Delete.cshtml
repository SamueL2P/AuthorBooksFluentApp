@model AuthorBooksFluentApp.Models.Author


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Delete</title>
</head>
<body>
    <h3>Are you sure you want to delete this?</h3>
    <div>
        
            <dt class="mt-3">
                @Html.DisplayNameFor(model => model.AuthorDetail)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.AuthorDetail)
            </dd>

        </dl>
    
        @using (Html.BeginForm()) {
            @Html.AntiForgeryToken()
    
            <div class="form-actions no-color">
                <input type="submit" value="Delete" class="btn btn-default bg-red-600 text-white mt-2 hover:bg-green-700" />
                <div>
                    @Html.ActionLink("Back to List", "Index")
                </div>
      
            </div>
        }
    </div>
</body>
</html>
